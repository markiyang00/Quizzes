@model TestPageViewModel
	<div>
		<strong>@Model.Test.Name</strong><br />
		@{
			var i = 1;
			<div>
				@{
					foreach (var question in Model.Questions)
					{
						<div>
							<strong>@i)@question.Text</strong>
							@{
								if (Model.Answers[i - 1] != null)
								{
									var j = 1;
									foreach (var answer in Model.Answers[i - 1])
									{
										<div>
											<strong>@j)@answer.Text</strong>
											<a class="btn btn-warning" asp-controller="Test" asp-action="Edit" asp-route-id="@answer.Id">Edit Answer</a>
											<a class="btn btn-warning" asp-controller="Test" asp-action="Del" asp-route-id="@answer.Id">Del Answer</a>
										</div>
										j++;
									}
								}
								<a class="btn btn-warning" asp-controller="Test" asp-action="Add" asp-route-id="@question.Id">Add Answer</a>
								<br />
							}
						</div>
						<a class="btn btn-warning" asp-controller="Test" asp-action="Edit" asp-route-id="@question.Id">Edit Question</a>
						<a class="btn btn-warning" asp-controller="Test" asp-action="Del" asp-route-id="@question.Id">Del Question</a><br/>
						i++;
					}

					<a class="btn btn-warning" asp-controller="Test" asp-action="Add" asp-route-testId="@Model.Test.Id">Add Question</a>
					<br />
				}
			</div>
		}
		<a class="btn btn-warning" asp-controller="Test" asp-action="Add">Add new Test</a>
		<a class="btn btn-warning" asp-controller="Admin" asp-action="Admin">Admin</a>
		<a class="btn btn-warning" asp-controller="Admin" asp-action="Exit">Exit</a>
	</div>